[project]
name = "ragstar"
version = "0.1.0"
description = "Repository discovery via vector search"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gitingest>=0.3.1",
    "chromadb>=0.4.0",
    "sentence-transformers>=2.2.0",
    "transformers>=4.30.0",
    "torch>=2.0.0",
    "requests>=2.31.0",
]
